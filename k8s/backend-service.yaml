apiVersion: v1
kind: Service
metadata:
  name: tienda-backend
  namespace: tienda
spec:
  type: NodePort
  selector:
    app: tienda-backend
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30081